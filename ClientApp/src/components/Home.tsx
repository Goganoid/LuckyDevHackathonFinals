import styled from "styled-components";

const Main = styled.div`
    position: absolute;
    min-width: 100%;
    min-heigth: 100%;

    background-color: #8639E8;
`;

export const Home = () => {
    return <Main />;
}